name: Generate metrics

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/**"
      - ".github/workflows/metrics.yml"

permissions:
  contents: write

concurrency:
  group: metrics
  cancel-in-progress: true

jobs:
  metrics:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate stats and language cards
        env:
          GITHUB_TOKEN: ${{ secrets.METRICS_TOKEN || secrets.GITHUB_TOKEN }}
          GH_USER: omarzone
        run: python scripts/gen_stats.py

      - name: Contribution calendar and streaks
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN || secrets.GITHUB_TOKEN }}
          user: omarzone
          template: classic
          filename: metrics/calendar.svg
          output_action: none
          base: ""
          config_timezone: America/Mexico_City
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

      - name: Achievements and coding habits
        continue-on-error: true
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN || secrets.GITHUB_TOKEN }}
          user: omarzone
          template: classic
          filename: metrics/achievements.svg
          output_action: none
          base: ""
          config_timezone: America/Mexico_City
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_threshold: C
          plugin_achievements_secrets: yes
          plugin_habits: yes
          plugin_habits_from: 200
          plugin_habits_days: 30
          plugin_habits_facts: yes
          plugin_habits_charts: yes
          plugin_habits_charts_type: classic

      - name: Guard against private names in generated cards
        run: |
          set -e
          if grep -ril -E "wopie|avis|veronica|voltrex|obd" metrics/*.svg; then
            echo "::error::Generated cards contain private names. Refusing to publish."
            git checkout -- metrics || true
            exit 1
          fi
          echo "Guard passed."

      - name: Commit generated cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add metrics/*.svg
          git diff --cached --quiet || git commit -m "chore: update metrics cards [skip ci]"
          git push
